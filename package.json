{
  "name": "nightlybuild",
  "version": "1.0.0",
  "private": true,
  "description": "Nightly Build storefront — a Courtright Collective sub-brand. Cloudflare Worker with static assets, Printify + Stripe.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "wrangler": "^3.90.0"
  }
}
